export const alertShow = reactive({
    toggleAlert : false,
    alertSuccess : false,
    alertWarning: false,
    alertError : false,
    alertMsg : "",
})

export let object = reactive({
    loading: false,
})

export let PCR = reactive({
    posts: [],
})
